import React from 'react';
import styles from './Feedback.scss';

const Feedback = () => (
  <section className={styles.section}>
    <h2>Feedback is paramount</h2>
    <h4>
      Early feedback through continuous delivery means there is less time wasted in features the users don't want and more time is dedicate for those value-adding features.
    </h4>
  </section>
);

export default Feedback;
